# 审批流程设计

<cite>
**本文档引用的文件**
- [PROJECT_CONTEXT.md](file://PROJECT_CONTEXT.md)
- [开题报告_精简版.md](file://开题报告_精简版.md)
- [docker-compose.yml](file://docker-compose.yml)
- [config/milvus_collection.yaml](file://config/milvus_collection.yaml)
- [sql/init.sql](file://sql/init.sql)
- [anomaly-detection-service/app/main.py](file://anomaly-detection-service/app/main.py)
- [anomaly-detection-service/app/api/routes/detection.py](file://anomaly-detection-service/app/api/routes/detection.py)
- [anomaly-detection-service/app/services/detection_service.py](file://anomaly-detection-service/app/services/detection_service.py)
- [anomaly-detection-service/app/core/pyod_detector.py](file://anomaly-detection-service/app/core/pyod_detector.py)
- [anomaly-detection-service/app/core/pysad_detector.py](file://anomaly-detection-service/app/core/pysad_detector.py)
- [anomaly-detection-service/app/models/schemas.py](file://anomaly-detection-service/app/models/schemas.py)
- [docs/prompts/shared-safety-constraints.md](file://docs/prompts/shared-safety-constraints.md)
</cite>

## 目录
1. [简介](#简介)
2. [项目结构](#项目结构)
3. [核心组件](#核心组件)
4. [架构概览](#架构概览)
5. [详细组件分析](#详细组件分析)
6. [依赖分析](#依赖分析)
7. [性能考虑](#性能考虑)
8. [故障排除指南](#故障排除指南)
9. [结论](#结论)
10. [附录](#附录)

## 简介

本文档为智能运维系统创建完整的审批流程设计文档。该系统基于 NetData 监控数据，构建多 Agent 协同的智能运维平台，具备自然语言问答、智能故障诊断和命令执行等功能。

系统采用 Orchestrator-Subagent 模式，其中 Execution Agent 负责生成命令、风险评估、人工审批和执行。审批流程设计重点关注基于角色的权限矩阵、自动化决策链路和安全控制机制。

## 项目结构

智能运维系统采用分层架构设计，包含以下主要组件：

```mermaid
graph TB
subgraph "前端层"
UI[Vue3 前端界面]
ApprovalUI[审批界面]
end
subgraph "应用层"
Backend[Spring Boot 后端]
Agent[多 Agent 协同]
Orchestrator[Orchestrator Agent]
QueryAgent[Query Agent]
AnalysisAgent[Analysis Agent]
ExecutionAgent[Execution Agent]
end
subgraph "服务层"
Milvus[Milvus 向量数据库]
MySQL[MySQL 关系数据库]
Redis[Redis 缓存]
Ollama[LLM 推理服务]
end
subgraph "基础设施"
NetData[NetData 监控系统]
PythonService[Python 异常检测服务]
end
UI --> Backend
ApprovalUI --> Backend
Backend --> Agent
Agent --> Orchestrator
Orchestrator --> QueryAgent
Orchestrator --> AnalysisAgent
Orchestrator --> ExecutionAgent
ExecutionAgent --> MySQL
ExecutionAgent --> Redis
ExecutionAgent --> Milvus
ExecutionAgent --> Ollama
PythonService --> Backend
NetData --> PythonService
```

**图表来源**
- [PROJECT_CONTEXT.md:120-149](file://PROJECT_CONTEXT.md#L120-L149)
- [docker-compose.yml:23-357](file://docker-compose.yml#L23-L357)

**章节来源**
- [PROJECT_CONTEXT.md:16-166](file://PROJECT_CONTEXT.md#L16-L166)
- [docker-compose.yml:1-357](file://docker-compose.yml#L1-357)

## 核心组件

### 审批流程核心组件

系统中的审批流程涉及多个核心组件，每个组件都有明确的职责分工：

#### 1. 用户权限管理系统
- **角色定义**：admin、operator、viewer、super-admin
- **权限矩阵**：基于角色的细粒度权限控制
- **安全约束**：输入验证、命令注入防护、URL 安全验证

#### 2. 命令执行审计系统
- **审计表结构**：完整的命令执行生命周期跟踪
- **状态管理**：pending、approved、rejected、executing、completed、failed
- **风险评估**：low、medium、high、critical 等级

#### 3. 审批控制机制
- **自动审批**：低风险命令的自动化处理
- **人工审批**：中高风险命令的分级审批
- **越权审批**：super-admin 的特殊权限

**章节来源**
- [docs/prompts/shared-safety-constraints.md:235-242](file://docs/prompts/shared-safety-constraints.md#L235-L242)
- [sql/init.sql:114-138](file://sql/init.sql#L114-L138)

## 架构概览

审批流程在整个系统架构中的位置和交互关系如下：

```mermaid
sequenceDiagram
participant User as 用户
participant Frontend as 前端界面
participant Backend as 后端服务
participant Approval as 审批服务
participant Audit as 审计系统
participant Command as 命令执行器
User->>Frontend : 提交执行请求
Frontend->>Backend : 发送审批申请
Backend->>Audit : 记录审计信息
Backend->>Approval : 检查权限和风险
alt 低风险命令
Approval->>Backend : 自动批准
Backend->>Command : 执行命令
else 需要人工审批
Approval->>Frontend : 发送待审批通知
User->>Frontend : 审批决策
Frontend->>Backend : 审批结果
Backend->>Audit : 更新审批状态
Backend->>Command : 执行命令
end
Command->>Audit : 记录执行结果
Audit->>Frontend : 更新状态显示
```

**图表来源**
- [开题报告_精简版.md:268-301](file://开题报告_精简版.md#L268-L301)
- [docs/prompts/shared-safety-constraints.md:235-242](file://docs/prompts/shared-safety-constraints.md#L235-L242)

## 详细组件分析

### 基于角色的审批权限矩阵

系统采用严格的基于角色的访问控制（RBAC）模型，定义了清晰的权限矩阵：

```mermaid
classDiagram
class Role {
+String role_name
+Set~Permission~ permissions
+boolean hasPermission(permission)
}
class Permission {
+String resource
+String action
+String level
}
class User {
+Long user_id
+String username
+String role
+Date created_at
}
class ApprovalMatrix {
+Map~Role, Set~Permission~~ matrix
+checkPermission(user, permission) boolean
+getApproverLevel(risk_level) int
}
Role --> Permission : contains
User --> Role : has
ApprovalMatrix --> Role : manages
ApprovalMatrix --> Permission : validates
```

**图表来源**
- [docs/prompts/shared-safety-constraints.md:235-242](file://docs/prompts/shared-safety-constraints.md#L235-L242)
- [sql/init.sql:26-41](file://sql/init.sql#L26-L41)

#### 角色权限详细定义

| 角色 | 知识问答 | 故障诊断 | 自动执行命令 | 审批执行命令 | 越权审批 |
|------|---------|---------|-------------|-------------|---------|
| viewer | ✅ | ✅ | ❌ | ❌ | ❌ |
| operator | ✅ | ✅ | ✅ | ✅ | ❌ |
| admin | ✅ | ✅ | ✅ | ✅ | ❌ |
| super-admin | ✅ | ✅ | ✅ | ✅ + 越权审批 | ✅ |

**章节来源**
- [docs/prompts/shared-safety-constraints.md:235-242](file://docs/prompts/shared-safety-constraints.md#L235-L242)

### 审批流程自动化设计

#### 风险评估引擎

系统实现了多层次的风险评估机制：

```mermaid
flowchart TD
Start([开始审批流程]) --> CollectInfo[收集命令信息]
CollectInfo --> ParseCommand[解析命令结构]
ParseCommand --> CheckTemplate{检查命令模板}
CheckTemplate --> |在模板中| TemplateRisk[模板风险评估]
CheckTemplate --> |不在模板中| CustomRisk[自定义命令分析]
TemplateRisk --> AnalyzePattern[分析命令模式]
CustomRisk --> AnalyzePattern
AnalyzePattern --> CheckWhitelist{检查白名单}
CheckWhitelist --> |在白名单| LowRisk[低风险]
CheckWhitelist --> |不在白名单| AnalyzeComponents[分析组件]
AnalyzeComponents --> CheckVariables{检查变量}
CheckVariables --> |安全变量| MediumRisk[中风险]
CheckVariables --> |危险变量| HighRisk[高风险]
LowRisk --> AutoApproval[自动审批]
MediumRisk --> ManualApproval[人工审批]
HighRisk --> EscalatedApproval[升级审批]
AutoApproval --> Execute[执行命令]
ManualApproval --> WaitApproval[等待审批]
EscalatedApproval --> WaitApproval
WaitApproval --> Approve{审批结果}
Approve --> |批准| Execute
Approve --> |拒绝| Reject[拒绝执行]
Execute --> RecordAudit[记录审计]
Reject --> RecordAudit
RecordAudit --> End([流程结束])
```

**图表来源**
- [开题报告_精简版.md:268-301](file://开题报告_精简版.md#L268-L301)
- [sql/init.sql:143-159](file://sql/init.sql#L143-L159)

#### 审批决策链路

```mermaid
sequenceDiagram
participant System as 系统
participant RiskEngine as 风险评估引擎
participant Auth as 权限验证
participant ApprovalQueue as 审批队列
participant User as 审批人
participant Audit as 审计系统
System->>RiskEngine : 评估命令风险
RiskEngine->>RiskEngine : 分析命令类型和参数
RiskEngine->>Auth : 验证用户权限
Auth->>Auth : 检查角色和资源权限
Auth->>ApprovalQueue : 放入审批队列
ApprovalQueue->>User : 发送审批通知
User->>ApprovalQueue : 审批决策
ApprovalQueue->>Audit : 记录审批结果
alt 审批通过
ApprovalQueue->>System : 允许执行
System->>System : 执行命令
else 审批拒绝
ApprovalQueue->>System : 拒绝执行
System->>Audit : 记录拒绝原因
end
```

**图表来源**
- [docs/prompts/shared-safety-constraints.md:235-242](file://docs/prompts/shared-safety-constraints.md#L235-L242)
- [sql/init.sql:114-138](file://sql/init.sql#L114-L138)

### 审批规则引擎实现

#### 命令模板系统

系统通过命令模板实现标准化的风险控制：

| 模板类别 | 命令示例 | 风险等级 | 白名单 |
|---------|---------|---------|--------|
| 状态检查 | systemctl status {{service_name}} | low | ✅ |
| 日志查看 | journalctl -u {{service_name}} -n {{lines:100}} | low | ✅ |
| 进程查询 | ps aux \| grep {{process_name}} | low | ✅ |
| 服务重启 | systemctl restart {{service_name}} | medium | ❌ |
| 日志清理 | find {{log_path}} -name "*.log" -mtime +{{days:7}} -delete | medium | ❌ |

#### 风险评估规则

```mermaid
flowchart TD
Command[命令输入] --> Parse[解析命令]
Parse --> ExtractVars[提取变量]
ExtractVars --> CheckDangerous{检查危险关键字}
CheckDangerous --> |发现危险| HighRisk[高风险]
CheckDangerous --> |无危险| CheckVariables
CheckVariables --> VarAnalysis[变量分析]
VarAnalysis --> CheckSources{检查来源}
CheckSources --> |可信来源| MediumRisk[中风险]
CheckSources --> |未知来源| HighRisk
HighRisk --> Block[阻止执行]
MediumRisk --> Approve[需要审批]
Block --> Log[记录日志]
Approve --> Log
Log --> End[结束]
```

**图表来源**
- [sql/init.sql:162-170](file://sql/init.sql#L162-L170)

**章节来源**
- [sql/init.sql:143-170](file://sql/init.sql#L143-L170)

### 权限验证机制

#### 输入安全验证

系统实现了多层安全验证机制：

```mermaid
flowchart TD
Input[用户输入] --> ValidateInput[输入验证]
ValidateInput --> CheckLength{长度检查}
CheckLength --> |超限| Reject[拒绝]
CheckLength --> |正常| CheckSQL{SQL注入检查}
CheckSQL --> |发现SQL注入| Reject
CheckSQL --> |安全| CheckCmd{命令注入检查}
CheckCmd --> |发现命令注入| Reject
CheckCmd --> |安全| CheckXSS{XSS检查}
CheckXSS --> |发现XSS| Reject
CheckXSS --> |安全| CheckURL{URL安全检查}
CheckURL --> |发现恶意URL| Reject
CheckURL --> |安全| Accept[接受请求]
Reject --> Log[记录安全事件]
Accept --> Process[继续处理]
Log --> End[结束]
```

**图表来源**
- [docs/prompts/shared-safety-constraints.md:182-230](file://docs/prompts/shared-safety-constraints.md#L182-L230)

#### URL 安全验证

系统实现了严格的 URL 安全验证：

```mermaid
classDiagram
class URLValidator {
+String[] allowed_domains
+validate_url(url) boolean
+add_allowed_domain(domain)
+remove_allowed_domain(domain)
}
class SecurityConfig {
+String[] ALLOWED_DOMAINS
+String[] BLOCKED_KEYWORDS
+int MAX_URL_LENGTH
}
class SafeURL {
+String original_url
+String validated_url
+boolean is_safe
+String validation_reason
}
URLValidator --> SecurityConfig : uses
URLValidator --> SafeURL : creates
```

**图表来源**
- [docs/prompts/shared-safety-constraints.md:185-195](file://docs/prompts/shared-safety-constraints.md#L185-L195)

**章节来源**
- [docs/prompts/shared-safety-constraints.md:172-230](file://docs/prompts/shared-safety-constraints.md#L172-L230)

### 流程控制逻辑

#### 审批状态管理

系统实现了完整的审批状态管理：

```mermaid
stateDiagram-v2
[*] --> Pending : 创建审批请求
Pending --> Approved : 自动批准
Pending --> Rejected : 拒绝执行
Pending --> AwaitingApproval : 人工审批
AwaitingApproval --> Approved : 审批通过
AwaitingApproval --> Rejected : 审批拒绝
AwaitingApproval --> Escalated : 升级审批
Approved --> Executing : 开始执行
Executing --> Completed : 执行成功
Executing --> Failed : 执行失败
Rejected --> [*] : 结束
Completed --> [*] : 结束
Failed --> [*] : 结束
Escalated --> AwaitingApproval : 重新审批
```

**图表来源**
- [sql/init.sql:124-124](file://sql/init.sql#L124-L124)

#### 审批队列管理

```mermaid
classDiagram
class ApprovalQueue {
+PriorityQueue~ApprovalRequest~ queue
+Map~String, Integer~ user_queue_count
+add_request(request)
+remove_request(request_id)
+get_next_request() ApprovalRequest
+get_user_requests(user_id) ApprovalRequest[]
}
class ApprovalRequest {
+String request_id
+Long user_id
+String command
+String risk_level
+Date created_at
+Date expires_at
+String status
+Integer priority
}
class ApprovalRule {
+String risk_level
+Integer max_wait_time
+String approver_role
+Boolean requires_escalation
}
ApprovalQueue --> ApprovalRequest : manages
ApprovalRequest --> ApprovalRule : follows
```

**图表来源**
- [sql/init.sql:114-138](file://sql/init.sql#L114-L138)

**章节来源**
- [sql/init.sql:114-138](file://sql/init.sql#L114-L138)

## 依赖分析

### 组件耦合关系

```mermaid
graph TB
subgraph "审批相关组件"
ApprovalEngine[审批引擎]
RiskAssessment[风险评估]
PermissionControl[权限控制]
AuditTrail[审计日志]
Notification[通知系统]
end
subgraph "外部依赖"
MySQL[MySQL 数据库]
Redis[Redis 缓存]
LLM[LLM 推理]
NetData[NetData 监控]
end
ApprovalEngine --> RiskAssessment
ApprovalEngine --> PermissionControl
ApprovalEngine --> AuditTrail
ApprovalEngine --> Notification
RiskAssessment --> MySQL
PermissionControl --> MySQL
AuditTrail --> MySQL
Notification --> Redis
ApprovalEngine --> LLM
RiskAssessment --> NetData
```

**图表来源**
- [docker-compose.yml:23-357](file://docker-compose.yml#L23-L357)
- [PROJECT_CONTEXT.md:120-149](file://PROJECT_CONTEXT.md#L120-L149)

### 数据流依赖

系统中的数据流依赖关系如下：

```mermaid
flowchart LR
subgraph "数据源"
NetData[NetData 监控数据]
UserInput[用户输入]
CommandTemplates[命令模板库]
end
subgraph "处理层"
RiskEngine[风险评估引擎]
PermissionEngine[权限验证引擎]
ApprovalEngine[审批决策引擎]
end
subgraph "存储层"
AuditDB[审计数据库]
ConfigDB[配置数据库]
Cache[缓存系统]
end
subgraph "输出层"
CommandExecutor[命令执行器]
NotificationSystem[通知系统]
Frontend[前端界面]
end
NetData --> RiskEngine
UserInput --> PermissionEngine
CommandTemplates --> RiskEngine
RiskEngine --> ApprovalEngine
PermissionEngine --> ApprovalEngine
ApprovalEngine --> AuditDB
ApprovalEngine --> Cache
ApprovalEngine --> NotificationSystem
ApprovalEngine --> CommandExecutor
AuditDB --> Frontend
Cache --> Frontend
```

**图表来源**
- [开题报告_精简版.md:268-301](file://开题报告_精简版.md#L268-L301)
- [sql/init.sql:114-138](file://sql/init.sql#L114-L138)

**章节来源**
- [docker-compose.yml:23-357](file://docker-compose.yml#L23-L357)
- [sql/init.sql:114-138](file://sql/init.sql#L114-L138)

## 性能考虑

### 审批流程性能优化

系统在审批流程中采用了多项性能优化策略：

1. **异步处理**：审批请求采用异步队列处理，避免阻塞主线程
2. **缓存机制**：频繁访问的权限信息和配置数据使用 Redis 缓存
3. **批量处理**：多个审批请求可以批量处理以提高效率
4. **负载均衡**：审批服务支持水平扩展以应对高并发场景

### 监控和统计功能

系统提供了完善的监控和统计功能：

```mermaid
graph TB
subgraph "监控指标"
ApprovalMetrics[审批指标]
RiskMetrics[风险指标]
PerformanceMetrics[性能指标]
end
subgraph "统计视图"
ApprovalStats[审批统计]
RiskTrends[风险趋势]
UserActivity[用户活动]
end
subgraph "告警机制"
ThresholdAlerts[阈值告警]
AnomalyDetection[异常检测]
EscalationAlerts[升级告警]
end
ApprovalMetrics --> ApprovalStats
RiskMetrics --> RiskTrends
PerformanceMetrics --> UserActivity
ApprovalStats --> ThresholdAlerts
RiskTrends --> AnomalyDetection
UserActivity --> EscalationAlerts
```

**图表来源**
- [sql/init.sql:249-274](file://sql/init.sql#L249-L274)

**章节来源**
- [sql/init.sql:249-274](file://sql/init.sql#L249-L274)

## 故障排除指南

### 审批异常处理机制

系统实现了多层次的异常处理机制：

```mermaid
flowchart TD
Error[审批异常] --> DetectError[检测异常]
DetectError --> ClassifyError[分类异常]
ClassifyError --> ValidationErrors[验证错误]
ClassifyError --> PermissionErrors[权限错误]
ClassifyError --> RiskErrors[风险错误]
ClassifyError --> SystemErrors[系统错误]
ValidationErrors --> LogValidation[记录验证错误]
PermissionErrors --> LogPermission[记录权限错误]
RiskErrors --> LogRisk[记录风险错误]
SystemErrors --> LogSystem[记录系统错误]
LogValidation --> NotifyUser[通知用户]
LogPermission --> NotifyUser
LogRisk --> NotifyUser
LogSystem --> NotifyAdmin[通知管理员]
NotifyUser --> Resolve[尝试解决]
NotifyAdmin --> Escalate[升级处理]
Resolve --> Retry[重试操作]
Retry --> Success[操作成功]
Retry --> Failure[操作失败]
Success --> End[结束]
Failure --> End
Escalate --> End
```

**图表来源**
- [docs/prompts/shared-safety-constraints.md:360-378](file://docs/prompts/shared-safety-constraints.md#L360-L378)

### 常见问题诊断

#### 审批流程常见问题

| 问题类型 | 症状 | 可能原因 | 解决方案 |
|---------|------|---------|---------|
| 审批超时 | 审批请求长时间未处理 | 审批队列拥堵、审批人离线 | 检查审批队列状态、通知审批人 |
| 权限不足 | 审批被拒绝 | 用户角色权限不够 | 提升用户角色、检查权限配置 |
| 风险评估错误 | 风险等级判定不准确 | 风险评估规则配置错误 | 调整风险评估参数、更新规则 |
| 系统异常 | 审批服务不可用 | 服务宕机、数据库连接失败 | 检查服务状态、重启服务 |

**章节来源**
- [docs/prompts/shared-safety-constraints.md:360-378](file://docs/prompts/shared-safety-constraints.md#L360-L378)

## 结论

本文档详细阐述了智能运维系统的审批流程设计，包括基于角色的权限矩阵、自动化审批决策链路、安全控制机制和监控统计功能。系统通过多层次的安全验证、严格的权限控制和完善的审计机制，确保了审批流程的安全性和可靠性。

审批流程设计充分考虑了实际运维场景的需求，既保证了操作的安全性，又提高了运维效率。通过自动化的风险评估和分级审批机制，系统能够在保证安全的前提下，最大化地减少人工干预，提高整体运维效率。

## 附录

### 审批流程配置选项

系统提供了灵活的配置选项，支持不同场景下的审批需求：

| 配置项 | 默认值 | 说明 |
|-------|--------|------|
| llm.provider | deepseek | LLM 提供商选择 |
| llm.model | deepseek-chat | LLM 模型名称 |
| execution.auto_approve_low_risk | true | 是否自动批准低风险命令 |
| execution.max_wait_time | 3600 | 命令执行最大等待时间（秒） |

**章节来源**
- [sql/init.sql:236-244](file://sql/init.sql#L236-L244)